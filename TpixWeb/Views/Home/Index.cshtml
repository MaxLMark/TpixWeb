@model TpixWeb.Models.ViewModels.IndexViewModel

<div class="text-center">
    @foreach (var category in Model.Categories)
    {
        @*<p>@category.Id</p>*@
        <a href="/c/@category.Title.ToLower()?catId=@category.Id">
            <h2>@category.Title</h2>
        </a>
    }
</div>
